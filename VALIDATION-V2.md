# 验证记录（2026-09-15，庭院细化）

- TypeScript：node node_modules/typescript/bin/tsc --noEmit，最终退出码 0。
- 生产构建：node "C:/Program Files/nodejs/node_modules/npm/bin/npm-cli.js" run build，最终退出码 0。
- 构建仍提示 Three.js 客户端分包超过 500 kB，为非阻塞警告；场景模块已动态加载。
- 浏览器控制台最终版本无 error。
- 树叶与花朵屋顶间距检查全部为 true；扩大叶片后，检查半径分别为 .37 和 .25。
- 四季陈列物的底部、前栏、后方与台面包围盒检查全部为 true。
- 桌面近景检查：正面格子推拉门、封闭侧墙与山墙、左右软垫、茶具棋盘、书架、柜面装饰、山水画均正常显示。
- 春、夏、秋、冬按钮逐一检查，诗句正确。仅存在晴与雨／雪两个天气按钮，默认晴天，手动选择后保持所选天气。
- 1280×800、390×844、844×390 视口检查，无横向溢出；Canvas 跟随视口尺寸。手机默认取景已按扩大的树冠调整。
- 草地为单个实例批次，桌面约 29940 片，粗指针设备使用 18000 片预算；草根不随风偏移，风场在 GPU 上运行。
- 原版 four-seasons 的 Git 已跟踪文件状态干净；全部本轮修改仅位于 four-seasons-v2。

这些检查属于本机浏览器与视口模拟，不代表真实手机长时间挂机的帧率、温度或耗电结果。

## 环境融合与点击互动验收
- 最终 TypeScript 检查退出码 0；最终生产构建退出码 0。
- node --test tests/interactions-v2.test.mjs：4 项通过，覆盖鼠标/触摸点击、拖动后回到原点、多指缩放、右键、长按、取消及失去捕获。
- 浏览器实点验证：猫回应、室内灯开关、石灯开关、溪流涟漪、植物摇晃、春季额外落花。
- 室内灯和右石灯分别记录独立状态；灯芯与灯具外围均可响应。拖动后 interactionCount 保持不变。
- 连续三次水面点击产生三组涟漪；春季点树产生 44 片额外花瓣；效果会消退并复用固定池。
- 1280×800、390×844、844×390 视口验证；无横向溢出，最终独立验证页控制台无 error。
- 全部屋顶树叶间距及四季陈列物边界检查保持通过。
- 兔子模型及其独立阴影已删除。小猫前移至未被推拉门遮挡的位置。
- 本机验证未进行真实手机长时间温度/电量测试。猫叫为 Web Audio 合成声音，非录音素材。

## 木廊、陈设与互动更新（2026-09-19）
- TypeScript 检查退出码 0；生产构建退出码 0。
- Sites 构建包装脚本因本机 npm 包装器路径错误退出；改用系统 npm-cli.js 执行相同 package build 脚本成功。
- `node --test tests/*.test.mjs`：6 项通过。新增检查覆盖有限草地顶点与法线、石灯和台阶距河岸净空、模型几何有效性、推拉门真实网格的开合拾取、盆栽点击隔离和落雪粒子回收。
- http://localhost:5174/ 返回 HTTP 200。
- 本轮未进行浏览器视觉验收或真实手机性能测试；上文浏览器检查记录对应此前版本，不代表本轮修改后画面已复验。
- 构建仍有超过 500 kB 的客户端 chunk 提示。门扇、盆栽木枝和茶具合并网格，花叶与雪采用实例绘制。
